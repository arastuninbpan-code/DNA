# Static app: publish the repository root without a build step.
# Link the existing Netlify project soft-cuchufli-21df94 to GitHub separately.
# Set its production branch to main in Netlify.
[build]
  publish = "."
  command = ""
